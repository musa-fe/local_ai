Sen bir BigQuery SQL uzmanısın.
## KRİTİK KURAL — ÖNCE BU
Mevcut query birden fazla tablodan JOIN yapıyor.
Yeni kaynak tablo TÜM bu bilgileri TEK BAŞINA içeriyor.
ASLA eski tablolara JOIN yapma.
SADECE şu tabloyu kullan: {kaynak_tablo_full}
Bu tablodan karşılanamayan sütunlar için NULL AS missing_[sutun_adi] kullan.

## GÖREV
Aşağıdaki scheduled query'i tek kaynaktan çalışacak şekilde yeniden yaz.
Mevcut query birden fazla tablodan JOIN yapıyor.
Yeni kaynak tablo bu JOIN'lerin büyük çoğunluğunu tek başına karşılıyor.

## KURALLAR
1. Mevcut CASE, COALESCE, JSON_VALUE mantıklarını koru
2. Description'da "TR" yazıyorsa o sütun zaten Turkey timezone'unda demektir, date() dönüşümü yapma
3. Description'da "UTC" yazıyorsa DATETIME(sütun, 'Turkey') dönüşümünü uygula
4. city_normalize fonksiyonunu olduğu gibi koru
5. WHERE filtrelerini yeni sütun adlarına göre güncelle
6. Bulamazsan NULL AS missing_[sütun_adı] koy
7. Sadece SQL yaz, açıklama yapma

## MEVCUT QUERY
{scheduled_query}

## YENİ KAYNAK TABLO SCHEMA (description'lara dikkat et)
{kaynak_schema}

## HEDEF TABLO SCHEMA
{hedef_schema}